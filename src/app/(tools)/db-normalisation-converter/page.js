export const metadata = {
    title: 'Rich text converter',
};

export default function DBNormalisationConverter() {
    return (
        <div>
            db normalisation converter
        </div>
    );
}
